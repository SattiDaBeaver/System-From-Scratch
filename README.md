# System-From-Scratch
Building a 32-bit RISCV CPU, with a custom OS, all from scratch.

## Python cocotb dependencies installation
sudo apt install -y verilator gcc-riscv64-unknown-elf binutils-riscv64-unknown-elf python3 python3-pip python3-venv